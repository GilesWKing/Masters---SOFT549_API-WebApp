@{
    @using Microsoft.AspNetCore.Http
    @inject IHttpContextAccessor HttpContextAccessor

    Layout = "_UserLayout";

    if (!Convert.ToBoolean(@HttpContextAccessor.HttpContext.Session.GetString("Authenticated")))
    {
        Context.Response.Redirect("/");
    }
    
}